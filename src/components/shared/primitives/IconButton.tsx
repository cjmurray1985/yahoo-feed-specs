import { IconButton as UdsIconButton, type IconButtonProps } from "@yahoo/uds";

const IconButton = (props: IconButtonProps) => <UdsIconButton {...props} />;

export default IconButton;
export type { IconButtonProps };
